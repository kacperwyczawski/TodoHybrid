@using Backend.Models

<div class="wrapper">
    @foreach (var item in Todos)
    {
        <TodoItem TodoItemParameter="item"/>
    }
</div>

@code {

    [Parameter]
    public required IEnumerable<ITodoItem> Todos { get; set; }

}